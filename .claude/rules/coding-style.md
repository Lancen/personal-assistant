# 代码风格规范

## TypeScript 规范

- 禁止使用 `any` 类型，必须使用明确的类型定义
- 启用严格的 TypeScript 配置
- 所有变量和函数都需要正确的类型标注

## 命名约定

- 组件使用 PascalCase 命名
- 函数和变量使用 camelCase 命名

## 包管理器

- 使用 pnpm（不要用 npm/yarn）
- 不要安装新依赖除非明确要求
